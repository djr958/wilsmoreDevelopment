import '../../temp/scripts/modernizr';  /* Generated using 'gulp modernizr' (see gulp/tasks/modernizr.js).*/
import 'picturefill';                   /* Ensures all browser support for tags like 'picture' and classes such as 'srcset' (IE doesn't support these).*/
import 'lazysizes';                     /* Allows lazy loading of images (load them only when they're needed). */

